<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>
        <variable
            name="vm"
            type="com.practice.smallbee.viewmodel.CoordinatorlayoutViewModel" />
        <variable
            name="adapter"
            type="com.practice.smallbee.adapter.CoordiantorAdapter" />
    </data>
            <androidx.core.widget.NestedScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent">
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="80dp"
                        android:text="@string/first_row_text"
                        android:gravity="center"
                        android:id="@+id/first_row_text"
                        android:background="@drawable/shape_radius25_colorf44336"
                        android:layout_margin="10dp"
                        app:layout_constraintTop_toTopOf="parent"/>
                    <Button
                        android:layout_width="match_parent"
                        android:layout_height="100dp"
                        android:background="@drawable/shape_radius25_color4caf50"
                        android:text="@string/button"
                        android:textSize="30sp"
                        android:layout_margin="10dp"
                        app:layout_constraintTop_toBottomOf="@id/first_row_text"
                        android:id="@+id/bt_button"/>
                    <LinearLayout
                        android:orientation="vertical"
                        android:id="@+id/main"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        app:layout_constraintTop_toBottomOf="@id/bt_button"
                        app:layout_constraintBottom_toBottomOf="parent">
                        <Button
                            android:layout_width="match_parent"
                            android:layout_height="50dp"
                            android:layout_marginStart="20dp"
                            android:layout_marginEnd="20dp"
                            android:id="@+id/bt_init"
                            android:background="@drawable/shape_radius25_colorf44336"
                            android:text="@{@string/init_weight(vm.number)}"
                            android:textSize="20sp"/>
                        <TextView
                            android:layout_width="match_parent"
                            android:layout_height="100dp"
                            android:id="@+id/tv_sport"
                            android:text="@{@string/click_times(vm.number)}"
                            app:layout_anchor="@id/bt_init"
                            app:layout_anchorGravity="bottom|left"
                            android:gravity="center"
                            android:textSize="60sp"
                            app:layout_behavior=".behavior.PullDownVisiableBehavior"
                            android:background="@drawable/shape_radius25_color4caf50"/>
                        <androidx.recyclerview.widget.RecyclerView
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:id="@+id/rv_scroll"
                            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                            android:orientation="vertical"
                            app:adapter="@{adapter}"/>
                    </LinearLayout>
                </androidx.constraintlayout.widget.ConstraintLayout>
            </androidx.core.widget.NestedScrollView>

</layout>